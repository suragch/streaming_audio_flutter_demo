name: streaming_audio_flutter_demo
description: A demo app for showing how to stream audio with Just Audio
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+2

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.12
  audio_video_progress_bar: ^0.9.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
